function refreshForm(innerHTML) {
  const newLikeForm = document.getElementById('like-project-<%=@project.id%>');
  newLikeForm.innerHTML = innerHTML;
}

function updateFavCount() {
   const badge = document.querySelector('.badge');
   badge.innerHTML = <%= current_user.favorites.count %>;
}


refreshForm('<%= j render "shared/like_unlike", project: @project, user: @user %>');
updateFavorite();
updateFavCount();
